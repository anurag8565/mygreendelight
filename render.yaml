services:
  # Real-Time Socket.io Server for Live Delivery GPS & Orders
  - type: web
    name: mygreendelight-socket
    runtime: node
    rootDir: socketserver
    buildCommand: npm install
    startCommand: npm start
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5000

